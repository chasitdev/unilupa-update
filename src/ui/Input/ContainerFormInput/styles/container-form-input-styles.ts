import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        paddingTop: 15,
        paddingLeft: 15,
        paddingBottom: 15,
        paddingRight: 15,
        borderRadius: 15,
        // zIndex: 9,
        
    }
})

export default styles;